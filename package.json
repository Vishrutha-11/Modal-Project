{
  "name": "modal-project",
  "version": "1.0.0",
  "private": true,
  "description": "A small HTML, CSS, and JavaScript modal window demo.",
  "scripts": {
    "lint": "node --check script.js",
    "test": "node --test",
    "build": "rm -rf dist && mkdir -p dist && cp index.html style.css script.js dist/"
  }
}
